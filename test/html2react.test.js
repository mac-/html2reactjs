var assert = require('assert'),
	HTML2React = require('../lib/html2react.js');

describe('HTML2React Unit Tests', function() {

	it('should throw an error when 2 or more options use the same character', function(done) {

		done();
	});

});